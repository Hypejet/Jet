package net.hypejet.jet.data.json.util;

/**
 * Represents a class with only one instance.
 *
 * @since 1.0
 */
public enum JsonUnit {
    /**
     * The instance.
     *
     * @since 1.0
     */
    INSTANCE
}